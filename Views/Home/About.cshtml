@model About

<h1>@Model.Title</h1>
<div class="alert alert-primary">@Model.Description</div>

<p>Tags:</p>


<ul>
@foreach(var tag in Model.Tags)
{
    <li>@tag</li>
}

</ul>


<div class="container">
    <div class="row">
        <div class="col-sm">
            One of three columns
        </div>
        <div class="col-sm">
            One of three columns
        </div>
        <div class="col-sm">
            One of three columns
        </div>
        <div class="col-sm">
            One of three columns
        </div>
        <div class="col-sm">
            One of three columns
        </div>
        <div class="col-sm">
            One of three columns
        </div>
    </div>
</div>

<p>Simple CRUD app in .NET</p>